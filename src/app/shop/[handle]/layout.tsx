/**
 * Shop Product Route Layout
 * 
 * Layout for the /shop/[handle] dynamic product routes
 */

export default function ProductLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
